using System.Collections.Generic;
using GeneralEnumList;
using UnityEngine;

public class InventoryPackaging : MonoBehaviour
{
    [SerializeField]
    List<GameObject> listOfSlot;

    public void AddFruit(FruitType fruitType)
    {
        //int i = 0;
        //while (i < listOfSlot.Count && listOfSlot[i].GetComponent<> != fruitType) { i++; }

        //if (i < listOfSlot.Count) listOfSlot[i].GetComponent<>;
    }
}
